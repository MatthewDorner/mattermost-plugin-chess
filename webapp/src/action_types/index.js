import {id as pluginId} from '../manifest';

export default {
    SET_CHALLENGEMODAL_VISIBILITY: `${pluginId}_set_challengemodal_visibility`,
    SET_GAMEMODAL_VISIBILITY: `${pluginId}_set_gamemodal_visibility`,
    SEND_GAME_POST: `${pluginId}_send_game_post`,
};
